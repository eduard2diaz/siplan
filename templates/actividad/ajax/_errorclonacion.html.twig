<div class="modal-dialog modal-full">
    <div class="modal-content">
        <div class="modal-header">
            <h4 class="modal-title">Errores de clonación</h4>
            <button type="button" class="close" data-dismiss="modal" aria-hidden="true"></button>
        </div>
        <div class="m--padding-30">
            <table class="table table-hover dataTable" id="table_erroresclonacion">
                <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Fecha de inicio</th>
                    <th>Fecha de fin</th>
                </tr>
                </thead>
                <tbody>
                {% for actividad in actividades %}
                    <tr>
                        <td>{{ actividad.nombre }}</td>
                        <td>{{ actividad.fecha }}</td>
                        <td>{{ actividad.fechaf }}</td>
                    </tr>
                {% endfor %}
                </tbody>
                <tfoot>
                <tr>
                    <th>Nombre</th>
                    <th>Fecha de inicio</th>
                    <th>Fecha de fin</th>
                </tr>
                </tfoot>
            </table>
        </div>
        <div class="modal-footer">
            <a class="btn btn-sm" data-dismiss="modal"><i class="flaticon-cancel"></i>Cancelar</a>
        </div>
    </div>
</div>


