<table class="table table-striped table-hover dataTable" id="table">
    <thead>
    <tr>
        <th>{{ 'row_number'|trans }}</th>
        <th>{{ 'name_field'|trans }}</th>
        <th>{{ 'area'|trans }}</th>
        <th></th>
    </tr>
    </thead>
    <tbody>
    {%set i=1%}
    {% for cargo in cargos %}
        <tr>
            <td>{{ i }}</td>
            <td>{{ cargo.nombre }}</td>
            <td>{{ cargo.area }}</td>
            <td>
                <ul class="list-inline">
                    <li>
                        <a class="btn blue btn-block btn-outline btn-sm edicion" data-href="{{ path('cargo_edit', { 'id': cargo.id }) }}"><i class="fa fa-edit"></i>{{ 'edit'|trans }}</a>
                    </li>
                    <li>
                        <a class="btn btn-danger btn-sm  eliminar_cargo" data-href="{{ path('cargo_delete', { 'id': cargo.id }) }}"><i class="fa fa-trash-o"></i>{{ 'delete'|trans }}</a>
                    </li>
                </ul>
            </td>
        </tr>
        {%set i=1+i%}
    {% endfor %}
    </tbody>
    <tfoot>
    <tr>
        <th>{{ 'row_number'|trans }}</th>
        <th>{{ 'name_field'|trans }}</th>
        <th>{{ 'area'|trans }}</th>
        <th></th>
    </tr>
    </tfoot>
</table>
