{% extends 'layout/userprofile.html.twig' %}
{% block content %}
    <div class="m-portlet m-portlet--mobile">
        <div class="m-portlet__head">
            <div class="m-portlet__head-caption">
                <div class="m-portlet__head-title">
                    <span class="m-portlet__head-icon"><i class="flaticon-list-3"></i></span>
                    <h3 class="m-portlet__head-text">
                        Consejo de dirección
                    </h3>
                </div>
            </div>

            <div class="m-portlet__head-tools">
                <ul class="m-portlet__nav">
                    <li class="m-portlet__nav-item">
                        <a data-href="{{ path('miembro_consejo_direccion_index') }}" class="btn btn-sm m-btn" id="miembroconsejo_tablerefrescar">
                            <span><i class="flaticon-refresh"></i></span> Refrescar
                        </a>
                    </li>
                    <li class="m-portlet__nav-item">
                        <a data-href="{{ path('miembro_consejo_direccion_new') }}" class="btn btn-sm btn-primary m-btn edicion text-uppercase"  data-toggle="m-tooltip" data-placement="top" title="" data-original-title="Registrar un nuevo miembro">
                            <span><i class="flaticon-add"></i></span> Nuevo
                        </a>
                    </li>
                </ul>
            </div>
        </div>
        <div class="m-portlet__body">
            <!--begin: Datatable -->
            {%include 'miembro_consejo_direccion/_table.html.twig'%}
            <!--end: Datatable -->
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{parent()}}
    <script src="{{ asset('script/entity/miembroconsejodireccion.js','siplan') }}"></script>
    <script>
        var total ={{miembro_consejo_direccions |length }}
            miembroconsejodireccion.init();
    </script>
{% endblock %}